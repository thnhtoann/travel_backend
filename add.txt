1.thêm djangorestframework-simplejwt==5.3.1 vào requirements.txt
2. travellous\setting.py
thêm 'accounts',
     'rest_framework_simplejwt', vào installedapp
 	
thêm AUTH_USER_MODEL = 'accounts.User'
thêm ở cuối
# Thay thế bằng thông tin email của bạn
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'your_email@gmail.com'
EMAIL_HOST_PASSWORD = 'your_app_password'
thêm ở đầu
from datetime import timedelta
thêm trong REST_FRAMEWORK
'DEFAULT_AUTHENTICATION_CLASSES': (
        'rest_framework_simplejwt.authentication.JWTAuthentication',
    ),
thêm ở cuối
SIMPLE_JWT = {
    "ACCESS_TOKEN_LIFETIME": timedelta(minutes=5), 
    "REFRESH_TOKEN_LIFETIME": timedelta(days=30), 
    "ROTATE_REFRESH_TOKENS": True,
    "BLACKLIST_AFTER_ROTATION": True,
}
3. travellous\urls.py
thêm path('api/accounts/', include('accounts.urls')), vào urlpatterns
4.thêm vào User trong api\model.py để tránh xung đột với account
    groups = models.ManyToManyField(
        'auth.Group',
        related_name='api_user_set', # Tên duy nhất 3
        blank=True,
        help_text=('The groups this user belongs to.'),
        related_query_name='user',
    )
    user_permissions = models.ManyToManyField(
        'auth.Permission',
        related_name='api_user_permissions_set', # Tên duy nhất 4
        blank=True,
        help_text=('Specific permissions for this user.'),
        related_query_name='user',
    )
5. thêm AUTHENTICATION_BACKENDS = [
    'django.contrib.auth.backends.ModelBackend',
]
OTP_EXPIRATION_MINUTES = 10
PASSWORD_HASHERS = [
    'django.contrib.auth.hashers.MD5PasswordHasher', 
    'django.contrib.auth.hashers.PBKDF2PasswordHasher', # Giữ lại thuật toán mặc định
] vào cuối setting.py


